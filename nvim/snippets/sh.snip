snippet cdsd
abbr    "cd \"$(dirname \"${BASH_SOURCE:-$0}\")\""
options head
  cd "$(dirname "${BASH_SOURCE:-\$0}")" || exit

snippet sd
abbr    "dirname \"${BASH_SOURCE:-$0}\""
options head
  dirname "${BASH_SOURCE:-\$0}"

snippet sbb
abbr    "#!/usr/bin/env bash"
options head
  #!/usr/bin/env bash

snippet reporoot
abbr    "$(git rev-parse --show-toplevel)"
  $(git rev-parse --show-toplevel)
